function fibonacciSeries(n) {
  var fib = [0, 1]; // initialize array with first two Fibonacci numbers
  for (var i = 2; i < n; i++) {
    fib[i] = fib[i - 1] + fib[i - 2]; // calculate the next Fibonacci number
  }
  return fib;
}
